// GOLDEN — the Delphi import unit for every examples/*.mls.

============================================================
=== add ===
============================================================
{ Generated by mlc - Mathless import unit for module 'add'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit add;

interface

const
  ML_MODULE = 'add.dll';
  ML_ADD_IFACE_HASH: UInt64 = UInt64($224A2953E2699E31);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_add(a: Integer; b: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== basket ===
============================================================
{ Generated by mlc - Mathless import unit for module 'basket'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.

  An array parameter is TWO parameters here: the pointer, then the length
  in ELEMENTS -- not bytes; ml_cap and ml_needed are bytes and these are not. The
  module borrows the memory for the call, never writes to it, never keeps it.
  - Pass a dynamic array as `@Arr[0], Length(Arr)`.
  - EMPTY arrays: `@Arr[0]` has no element 0. With range checking off (the
    release default) it does not raise - it yields the array's own nil pointer,
    which is what a length of 0 wants, because the module never dereferences.
    Under the $R+ directive the same line RAISES. So write it as:
      if Length(Arr) = 0 then P := nil else P := @Arr[0];
  - An index outside 0..len-1 returns ML_ST_INDEX_OUT_OF_RANGE and writes no
    out-parameter.
  }
unit basket;

interface

const
  ML_MODULE = 'basket.dll';
  ML_BASKET_IFACE_HASH: UInt64 = UInt64($5D04BA666CC0CBC6);
  ML_BASKET_ERR_E_LENGTH_MISMATCH = 1;
  { An array index outside 0..len. The out-parameter is NOT written (D17), and
    the length is the one YOU passed alongside the pointer. }
  ML_ST_INDEX_OUT_OF_RANGE = -2;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_basket_total(qty: PInteger; qty_len: Integer; price: PInteger; price_len: Integer; cap: Integer; out out_value: Integer): Integer; cdecl; external ML_MODULE;
function mlx_largest(xs: PDouble; xs_len: Integer; floor: Double; out out_value: Double): Integer; cdecl; external ML_MODULE;
function mlx_how_many(flags: PBoolean; flags_len: Integer): Integer; cdecl; external ML_MODULE;
function mlx_any_set(flags: PBoolean; flags_len: Integer; out out_value: Boolean): Integer; cdecl; external ML_MODULE;
function mlx_pick(xs: PInteger; xs_len: Integer; idx: Integer; out out_value: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== carrier ===
============================================================
{ Generated by mlc - Mathless import unit for module 'carrier'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.

  PAnsiChar is NOT Delphi's `string`, which is UTF-16 (UnicodeString). The module
  compares BYTES up to the NUL and never inspects the encoding (SPEC-string-input
  DP-S2), so how you build the argument decides whether it can match at all.
  MEASURED on Delphi 37.0 Win64 through this ABI, sending "UPSN":
  - PAnsiChar(AnsiString(S)) - CORRECT. Sends 55 50 53 4E 00. Inline as an
    argument is fine (measured): the temporary a cast expression makes lives to
    the end of the STATEMENT, and the call is that statement. What you must not
    do is KEEP the pointer - `P := PAnsiChar(AnsiString(S))` and use P in a later
    statement reads a dead temporary. That failure is undefined behaviour, so it
    is stated as a rule and not claimed as measured.
  - PAnsiChar(S) on a UnicodeString - WRONG. It does NOT convert. It sends the
    UTF-16 bytes 55 00 50 00 53 00 4E 00, identical to the Pointer spelling below,
    so the module reads to the first NUL and sees ONE character. The compiler does
    object - W1044 Suspicious typecast of string to PAnsiChar - so it is wrong but
    not silent.
  - PAnsiChar(Pointer(S)) - WRONG and SILENT. The same UTF-16 bytes, and dcc64
    says nothing about it at all. Nothing crashes.
  Plain `string` IS UnicodeString in Delphi, so PAnsiChar(S) on one is the SECOND
  line, not the first. Free Pascal in -Mdelphi mode does not emulate that - its
  `string` is AnsiString, so identical source is correct there and wrong here.
  Measured both ways, which is why an FPC run cannot stand in for a Delphi one.
  With an EXPLICIT UnicodeString, Free Pascal sends the same bytes as Delphi
  (measured), so the trap itself is not Embarcadero-only - what differs is what
  `string` means.
  }
unit carrier;

interface

const
  ML_MODULE = 'carrier.dll';
  ML_CARRIER_IFACE_HASH: UInt64 = UInt64($C8D1191E339AAF6E);
  ML_CARRIER_ERR_E_UNKNOWN_SCAC = 1;
  { Q12 caller-allocates protocol: the buffer was too small to hold the result,
    NUL included. Truncation is a FAILURE, not a short success - nothing is written,
    and ml_needed is the exact size to allocate, in the same unit as ml_cap. }
  ML_ST_INSUFFICIENT_BUFFER = -1;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_carrier_name(scac: PAnsiChar; ml_buf: PByte; ml_cap: Integer; out ml_needed: Integer): Integer; cdecl; external ML_MODULE;
function mlx_carrier_label(scac: PAnsiChar; out tier: Integer; ml_buf: PByte; ml_cap: Integer; out ml_needed: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== commission ===
============================================================
{ Generated by mlc - Mathless import unit for module 'commission'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit commission;

interface

const
  ML_MODULE = 'commission.dll';
  ML_COMMISSION_IFACE_HASH: UInt64 = UInt64($8120E9C099B13F94);
  ML_COMMISSION_ERR_E_NEGATIVE = 1;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_commission(amount: Double; out tier: Integer): Double; cdecl; external ML_MODULE;
function mlx_commission_checked(amount: Double; out tier: Integer; out out_value: Double): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== count_bounded ===
============================================================
{ Generated by mlc - Mathless import unit for module 'count_bounded'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit count_bounded;

interface

const
  ML_MODULE = 'count_bounded.dll';
  ML_COUNT_BOUNDED_IFACE_HASH: UInt64 = UInt64($E83F762BFBFC9CA4);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_count_bounded(n: Integer; cap: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== deduction ===
============================================================
{ Generated by mlc - Mathless import unit for module 'deduction'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit deduction;

interface

const
  ML_MODULE = 'deduction.dll';
  ML_DEDUCTION_IFACE_HASH: UInt64 = UInt64($B2B1F335D1DC7105);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_deduction(gross: Double): Double; cdecl; external ML_MODULE;
function mlx_fl(x: Double): Double; cdecl; external ML_MODULE;
function mlx_ce(x: Double): Double; cdecl; external ML_MODULE;
function mlx_ro(x: Double): Double; cdecl; external ML_MODULE;
function mlx_tr(x: Double): Double; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== discount ===
============================================================
{ Generated by mlc - Mathless import unit for module 'discount'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit discount;

interface

const
  ML_MODULE = 'discount.dll';
  ML_DISCOUNT_IFACE_HASH: UInt64 = UInt64($05697A6FAFD68344);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_discount(price: Double; vip: Boolean): Double; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== discount2 ===
============================================================
{ Generated by mlc - Mathless import unit for module 'discount2'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit discount2;

interface

const
  ML_MODULE = 'discount2.dll';
  ML_DISCOUNT2_IFACE_HASH: UInt64 = UInt64($EE1B0032D821B816);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_discount2(price: Double; vip: Boolean): Double; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== discount3 ===
============================================================
{ Generated by mlc - Mathless import unit for module 'discount3'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit discount3;

interface

const
  ML_MODULE = 'discount3.dll';
  ML_DISCOUNT3_IFACE_HASH: UInt64 = UInt64($0532D4F36E005A3B);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_discount3(price: Double; vip: Boolean): Double; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== discount4 ===
============================================================
{ Generated by mlc - Mathless import unit for module 'discount4'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit discount4;

interface

const
  ML_MODULE = 'discount4.dll';
  ML_DISCOUNT4_IFACE_HASH: UInt64 = UInt64($5C3F47E3EB635EF8);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_discount4(price: Double; vip: Boolean): Double; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== line_total ===
============================================================
{ Generated by mlc - Mathless import unit for module 'line_total'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit line_total;

interface

const
  ML_MODULE = 'line_total.dll';
  ML_LINE_TOTAL_IFACE_HASH: UInt64 = UInt64($2B4FBE9547EC2453);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_line_total(each: Double; qty: Integer): Double; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== negate_if ===
============================================================
{ Generated by mlc - Mathless import unit for module 'negate_if'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit negate_if;

interface

const
  ML_MODULE = 'negate_if.dll';
  ML_NEGATE_IF_IFACE_HASH: UInt64 = UInt64($F05D0C1F381CF156);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_negate_if(x: Integer; flip: Boolean): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== pack ===
============================================================
{ Generated by mlc - Mathless import unit for module 'pack'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit pack;

interface

const
  ML_MODULE = 'pack.dll';
  ML_PACK_IFACE_HASH: UInt64 = UInt64($DA0077B3A04515AB);
  ML_PACK_ERR_E_EMPTY_BOX = 1;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_boxes(qty: Integer; per_box: Integer): Integer; cdecl; external ML_MODULE;
function mlx_loose(qty: Integer; per_box: Integer): Integer; cdecl; external ML_MODULE;
function mlx_boxes_checked(qty: Integer; per_box: Integer; out out_value: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== quote ===
============================================================
{ Generated by mlc - Mathless import unit for module 'quote'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit quote;

interface

const
  ML_MODULE = 'quote.dll';
  ML_QUOTE_IFACE_HASH: UInt64 = UInt64($159CAA5FE5CB3400);
  ML_QUOTE_ERR_E_BAD_QTY = 1;
  ML_QUOTE_ERR_E_DIV0 = 2;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_unit_price(total: Double; qty: Integer; out out_value: Double): Integer; cdecl; external ML_MODULE;
function mlx_line_check(qty: Integer; out tier: Integer; out out_value: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== receipt ===
============================================================
{ Generated by mlc - Mathless import unit for module 'receipt'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.

  PAnsiChar is NOT Delphi's `string`, which is UTF-16 (UnicodeString). The module
  compares BYTES up to the NUL and never inspects the encoding (SPEC-string-input
  DP-S2), so how you build the argument decides whether it can match at all.
  MEASURED on Delphi 37.0 Win64 through this ABI, sending "UPSN":
  - PAnsiChar(AnsiString(S)) - CORRECT. Sends 55 50 53 4E 00. Inline as an
    argument is fine (measured): the temporary a cast expression makes lives to
    the end of the STATEMENT, and the call is that statement. What you must not
    do is KEEP the pointer - `P := PAnsiChar(AnsiString(S))` and use P in a later
    statement reads a dead temporary. That failure is undefined behaviour, so it
    is stated as a rule and not claimed as measured.
  - PAnsiChar(S) on a UnicodeString - WRONG. It does NOT convert. It sends the
    UTF-16 bytes 55 00 50 00 53 00 4E 00, identical to the Pointer spelling below,
    so the module reads to the first NUL and sees ONE character. The compiler does
    object - W1044 Suspicious typecast of string to PAnsiChar - so it is wrong but
    not silent.
  - PAnsiChar(Pointer(S)) - WRONG and SILENT. The same UTF-16 bytes, and dcc64
    says nothing about it at all. Nothing crashes.
  Plain `string` IS UnicodeString in Delphi, so PAnsiChar(S) on one is the SECOND
  line, not the first. Free Pascal in -Mdelphi mode does not emulate that - its
  `string` is AnsiString, so identical source is correct there and wrong here.
  Measured both ways, which is why an FPC run cannot stand in for a Delphi one.
  With an EXPLICIT UnicodeString, Free Pascal sends the same bytes as Delphi
  (measured), so the trap itself is not Embarcadero-only - what differs is what
  `string` means.
  }
unit receipt;

interface

const
  ML_MODULE = 'receipt.dll';
  ML_RECEIPT_IFACE_HASH: UInt64 = UInt64($12CC5B502BEA4895);
  ML_RECEIPT_ERR_E_NO_ITEMS = 1;
  { Q12 caller-allocates protocol: the buffer was too small to hold the result,
    NUL included. Truncation is a FAILURE, not a short success - nothing is written,
    and ml_needed is the exact size to allocate, in the same unit as ml_cap. }
  ML_ST_INSUFFICIENT_BUFFER = -1;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_full_name(first: PAnsiChar; last: PAnsiChar; ml_buf: PByte; ml_cap: Integer; out ml_needed: Integer): Integer; cdecl; external ML_MODULE;
function mlx_receipt_line(item: PAnsiChar; qty: Integer; unit_price: Integer; ml_buf: PByte; ml_cap: Integer; out ml_needed: Integer): Integer; cdecl; external ML_MODULE;
function mlx_label(n: Integer; ml_buf: PByte; ml_cap: Integer; out ml_needed: Integer): Integer; cdecl; external ML_MODULE;
function mlx_summary(count: Integer; ml_buf: PByte; ml_cap: Integer; out ml_needed: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== refund ===
============================================================
{ Generated by mlc - Mathless import unit for module 'refund'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit refund;

interface

const
  ML_MODULE = 'refund.dll';
  ML_REFUND_IFACE_HASH: UInt64 = UInt64($C22D9C0A1441EE54);
  ML_REFUND_ERR_E_NEG = 3;
  ML_REFUND_ERR_E_TOO_LATE = 4;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_refund(paid: Double; days_since: Integer; out out_value: Double): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== safe_div ===
============================================================
{ Generated by mlc - Mathless import unit for module 'safe_div'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit safe_div;

interface

const
  ML_MODULE = 'safe_div.dll';
  ML_SAFE_DIV_IFACE_HASH: UInt64 = UInt64($91118FB2AED9DEE5);
  ML_SAFE_DIV_ERR_DIV_BY_ZERO = 1;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_safe_div(a: Double; b: Double; out out_value: Double): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== shapes ===
============================================================
{ Generated by mlc - Mathless import unit for module 'shapes'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.

  PAnsiChar is NOT Delphi's `string`, which is UTF-16 (UnicodeString). The module
  compares BYTES up to the NUL and never inspects the encoding (SPEC-string-input
  DP-S2), so how you build the argument decides whether it can match at all.
  MEASURED on Delphi 37.0 Win64 through this ABI, sending "UPSN":
  - PAnsiChar(AnsiString(S)) - CORRECT. Sends 55 50 53 4E 00. Inline as an
    argument is fine (measured): the temporary a cast expression makes lives to
    the end of the STATEMENT, and the call is that statement. What you must not
    do is KEEP the pointer - `P := PAnsiChar(AnsiString(S))` and use P in a later
    statement reads a dead temporary. That failure is undefined behaviour, so it
    is stated as a rule and not claimed as measured.
  - PAnsiChar(S) on a UnicodeString - WRONG. It does NOT convert. It sends the
    UTF-16 bytes 55 00 50 00 53 00 4E 00, identical to the Pointer spelling below,
    so the module reads to the first NUL and sees ONE character. The compiler does
    object - W1044 Suspicious typecast of string to PAnsiChar - so it is wrong but
    not silent.
  - PAnsiChar(Pointer(S)) - WRONG and SILENT. The same UTF-16 bytes, and dcc64
    says nothing about it at all. Nothing crashes.
  Plain `string` IS UnicodeString in Delphi, so PAnsiChar(S) on one is the SECOND
  line, not the first. Free Pascal in -Mdelphi mode does not emulate that - its
  `string` is AnsiString, so identical source is correct there and wrong here.
  Measured both ways, which is why an FPC run cannot stand in for a Delphi one.
  With an EXPLICIT UnicodeString, Free Pascal sends the same bytes as Delphi
  (measured), so the trap itself is not Embarcadero-only - what differs is what
  `string` means.
  }
unit shapes;

interface

const
  ML_MODULE = 'shapes.dll';
  ML_SHAPES_IFACE_HASH: UInt64 = UInt64($EBBDE5676601568D);
  ML_SHAPES_ERR_E_NEG = 1;
  ML_SHAPES_ERR_E_ODD = 2;
  { Q12 caller-allocates protocol: the buffer was too small to hold the result,
    NUL included. Truncation is a FAILURE, not a short success - nothing is written,
    and ml_needed is the exact size to allocate, in the same unit as ml_cap. }
  ML_ST_INSUFFICIENT_BUFFER = -1;

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_is_big(n: Integer; out out_value: Boolean): Integer; cdecl; external ML_MODULE;
function mlx_bump(n: Integer; out out_value: Integer): Integer; cdecl; external ML_MODULE;
function mlx_answer(out out_value: Integer): Integer; cdecl; external ML_MODULE;
function mlx_pi_ish: Double; cdecl; external ML_MODULE;
function mlx_span(n: Integer; out lo: Integer; out hi: Integer): Integer; cdecl; external ML_MODULE;
function mlx_split(x: Double; out whole: Double; out negative: Boolean): Double; cdecl; external ML_MODULE;
function mlx_grade(n: Integer; out tier: Integer; out out_value: Integer): Integer; cdecl; external ML_MODULE;
function mlx_code_of(s: PAnsiChar; out out_value: Integer): Integer; cdecl; external ML_MODULE;
function mlx_tag(n: Integer; ml_buf: PByte; ml_cap: Integer; out ml_needed: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== sum_to ===
============================================================
{ Generated by mlc - Mathless import unit for module 'sum_to'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.
  }
unit sum_to;

interface

const
  ML_MODULE = 'sum_to.dll';
  ML_SUM_TO_IFACE_HASH: UInt64 = UInt64($D3FC526CA957505A);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_sum_to(n: Integer): Integer; cdecl; external ML_MODULE;

implementation

end.

============================================================
=== vat ===
============================================================
{ Generated by mlc - Mathless import unit for module 'vat'. Do not edit.
  This GENERATOR was verified once against a real Delphi host on 2026-09-07:
  dcc64 compiled its units for Win64 and the host called the modules over the C
  ABI (14 checks, GATE_DELPHI_OK). BY HAND AND NOT GATED - the edition available
  refuses command-line builds, so nothing repeats it. Treat a change to the
  generator as unverified until someone repeats it.
  Boolean is 1 byte to match the module ABI; do not use LongBool.

  PAnsiChar is NOT Delphi's `string`, which is UTF-16 (UnicodeString). The module
  compares BYTES up to the NUL and never inspects the encoding (SPEC-string-input
  DP-S2), so how you build the argument decides whether it can match at all.
  MEASURED on Delphi 37.0 Win64 through this ABI, sending "UPSN":
  - PAnsiChar(AnsiString(S)) - CORRECT. Sends 55 50 53 4E 00. Inline as an
    argument is fine (measured): the temporary a cast expression makes lives to
    the end of the STATEMENT, and the call is that statement. What you must not
    do is KEEP the pointer - `P := PAnsiChar(AnsiString(S))` and use P in a later
    statement reads a dead temporary. That failure is undefined behaviour, so it
    is stated as a rule and not claimed as measured.
  - PAnsiChar(S) on a UnicodeString - WRONG. It does NOT convert. It sends the
    UTF-16 bytes 55 00 50 00 53 00 4E 00, identical to the Pointer spelling below,
    so the module reads to the first NUL and sees ONE character. The compiler does
    object - W1044 Suspicious typecast of string to PAnsiChar - so it is wrong but
    not silent.
  - PAnsiChar(Pointer(S)) - WRONG and SILENT. The same UTF-16 bytes, and dcc64
    says nothing about it at all. Nothing crashes.
  Plain `string` IS UnicodeString in Delphi, so PAnsiChar(S) on one is the SECOND
  line, not the first. Free Pascal in -Mdelphi mode does not emulate that - its
  `string` is AnsiString, so identical source is correct there and wrong here.
  Measured both ways, which is why an FPC run cannot stand in for a Delphi one.
  With an EXPLICIT UnicodeString, Free Pascal sends the same bytes as Delphi
  (measured), so the trap itself is not Embarcadero-only - what differs is what
  `string` means.
  }
unit vat;

interface

const
  ML_MODULE = 'vat.dll';
  ML_VAT_IFACE_HASH: UInt64 = UInt64($7CE7BE3A0EEE106C);

function ml_module_abi_version: LongWord; cdecl; external ML_MODULE;
function ml_iface_hash: UInt64; cdecl; external ML_MODULE;
function mlx_vat_rate(country: PAnsiChar): Double; cdecl; external ML_MODULE;
function mlx_issuer_of(bin: PAnsiChar): Integer; cdecl; external ML_MODULE;
function mlx_is_export_item(code: PAnsiChar): Boolean; cdecl; external ML_MODULE;

implementation

end.
