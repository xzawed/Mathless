# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.vscode/
.idea/

# Rust / cargo build output
/target/
**/*.rs.bk

# Future build output (no build system yet for non-Rust parts)
/build/
/out/
*.o
*.obj
*.dll
*.so
*.dylib

# MSVC leftovers. `vcvars64.bat` writes vc.log into the working directory, so an
# acceptance-D run drops one in the repository root — and #147 committed it, because
# `git add -A` cannot tell a build artifact from a source file.
vc.log
*.exe
*.lib
*.pdb
*.ilk
