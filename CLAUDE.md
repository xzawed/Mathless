# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# Mathless — Claude / AI 에이전트 작업 규칙

이 프로젝트는 아직 구현보다 **개념을 깨지 않는 것**이 우선이다.

## 저장소 성격

- **설계 문서 저장소다. 아직 구현 코드가 없다.** `src/`, 빌드 스크립트, 테스트, 패키지 매니페스트가 없으므로 build/lint/test 명령도 없다.
- 산출물은 `docs/*.md`와 루트의 `README.md` / `CLAUDE.md`뿐이다.
- 작업의 기본 형태는 **문서를 읽고, 모순을 찾고, 열린 질문을 닫을 대안을 제시하는 것**이다. 코드 생성이 아니다.

## 문서를 읽는 순서

`README.md`의 문서 지도가 정본이다. 새 세션은 이 순서로 읽는다:

`CLAUDE.md`(이 파일) → `docs/VISION.md` → `docs/DECISIONS.md` → `docs/ARCHITECTURE.md` → `docs/LANGUAGE.md` → `docs/HOST_ABI.md` → `docs/SECURITY.md` → `docs/ROADMAP.md` → `docs/OPEN_QUESTIONS.md` → `docs/COMPETITIVE.md` → `docs/GLOSSARY.md`

## 큰 그림 (여러 문서를 종합한 아키텍처)

핵심 정체성은 한 문장이다: **컴파일 타임 변환 + 네이티브 출력 + C ABI 호스트 연동으로, 호스트 재빌드 없이 타입 있는 로직을 보호된 네이티브 모듈로 붙인다.**

파이프라인(모두 컴파일 타임, 런타임 해석 없음):

```
표면 문법 .mls  →  parse/typecheck  →  내부 IR  →  네이티브 codegen  →  모듈 .mll/.dll/.so  →  [C ABI]  →  호스트(Delphi/C/C++/C#…)
```

세 가지가 서로 맞물려 있어 하나만 봐선 안 된다:

- **이중 계층** (`LANGUAGE.md`): *Surface*(넓은 개발자층용 표면 문법, C#에 가까운 방향 제안·미확정)와 *Meaning*(강한 정적 타입·네이티브 lowering에 가까운 의미 모델)를 분리한다. "내부가 Delphi"는 Delphi 컴파일러를 부른다는 뜻이 **아니라** 의미 모델이 Object Pascal/네이티브에 가깝다는 뜻이다.
- **C ABI가 유일한 1급 경계** (`HOST_ABI.md`): 언어별 바인딩은 C ABI 위의 얇은 래퍼다. MVP 마샬링은 정수/실수/bool/C 문자열/합의된 레이아웃의 구조체 포인터/1단계 콜백으로 제한. 모듈은 호스트 메모리를 소유하지 않는다. 예외가 ABI를 가로지르지 않게 한다.
- **보호는 단계적** (`SECURITY.md`): 목표는 "리버싱 불가"가 아니라 분석·변조 비용을 높이는 것. P0(MVP)=소스 미배포+심볼 strip+최소 export, P1=무결성/화이트리스트, P2=난독화. 개발 빌드와 배포 빌드의 보호 수준을 분리한다.

## 반드시 지킬 것

1. `docs/DECISIONS.md`의 확정 결정(D01~D13)을 뒤집지 않는다. 뒤집으려면 그 문서에 근거와 트레이드오프를 **먼저** 쓴다.
2. 런타임 인터프리터/자체 VM을 기본 실행 모델로 도입하지 않는다 (R01 기각).
3. 소스를 배포 산출물로 만들지 않는다 (R02 기각).
4. 호스트 경계를 C가 아닌 언어별 ABI로 바로 쪼개지 않는다. 1차 경계는 C ABI다.
5. 웹 지원, 제네릭, 패키지 매니저, 최고 수준 난독화를 MVP에 넣지 않는다.
6. "리버싱 불가능"이라고 대외 문구를 쓰지 않는다. 사용 표현은 "분석과 변조 비용을 높인다".
7. 성능 monologue보다 **컴파일 타임 변환 + 네이티브 출력**을 유지한다.
8. **`DECISIONS.md`는 사용자 확인 없이 바꾸지 않는다.** `OPEN_QUESTIONS.md`의 Q1~Q5에 결정안을 제안할 때는 장단점 표를 쓰고 문서 수정 전에 확인을 받는다.

## Grok 협업과 실측 기반 원칙 (필수)

프로세스 규칙이다. `DECISIONS.md`를 바꾸지 않는다. 기존 규칙 6·7과 산출물 규칙(추측 → `OPEN_QUESTIONS.md`)을 재서술하지 않고, **협업 방식**과 **근거 수준**을 고정한다.

### Grok 협업 (처음부터 끝까지)

- 모든 실질 작업은 착수 → 진행 → 완료 전 검증까지 Grok과 협업한다. 혼자 끝내지 않는다.
- 착수 시 접근안을 Grok과 공유한다(`grok_build_plan`). 구현·진단·코드 검토는 완료를 선언하기 전 반드시 Grok으로 2차 검증한다(`grok_build_verify`).
- Phase 0(문서만) 단계에서 Grok은 **교차 확인 수단**이다. 설계 결정(Q1~Q5 포함)의 최종 승인자는 사용자이며, 아키텍처·보안 결정을 `grok_build_delegate`에 위임하지 않는다.
- Grok 도구가 실패하면 직접 대체 수행하지 않고, 사용자에게 오류를 보고한 뒤 판단을 구한다. 단, 이 실패는 **구현 작업의 완료 선언만** 막는다. 문서 정합 작업(모순 탐지, 대안 장단점 표)은 계속 진행한다.

### 실측 데이터 기반 (수행·검증 모두 필수)

결정·주장·완료 판단은 추측이 아니라 **그 단계에서 쓸 수 있는 가장 강한 실측·근거**에 묶는다. 없는 측정값을 지어내지 않는다.

| 수준 | 언제 | 예 |
|------|------|-----|
| E0 문서 정합 | Phase 0부터 | Dxx/Qn 인용, 모순 없음, 가정 명시, Q1~Q5 장단점 표 |
| E1 외부 실측·근거 | Phase 0부터 (출처 필수) | 기존 C ABI·로더·심볼 도구(`nm`/`dumpbin` 등)의 문서화·검증된 동작. Mathless 자체 측정이 아님을 명시 |
| E2 산출물 실측 | Phase 1+ (코드가 생기면 필수) | 실제 컴파일 산출물, C ABI 호출 반환값, strip 전후 심볼 덤프, 모듈 로드 성공/실패 로그, 벤치마크 수치 |

- 설계 선택(Q1~Q5)은 E0(+필요 시 E1)과 사용자 확인으로 닫는다. 이는 트레이드오프이지 측정 대상이 아니므로, E2가 없다는 이유로 되돌리지 않는다.
- 제품 속성("더 빠르다", "보호된다")은 **E2 실측 없이 확정 문장으로 쓰지 않는다.** 가정은 가정으로 표시하거나 `OPEN_QUESTIONS.md`로 보낸다.
- **완료 검증은 그 단계의 최고 근거로 실측한다.** Phase 0의 완료 검증은 E0(문서 정합), 코드가 생긴 뒤의 완료 검증은 E2(실행 결과·데이터)를 반드시 포함한다.
- 보호 관련 측정은 **프록시만** 적는다: export 개수, 디버그 심볼 유무, 배포물의 소스 부재, (P1) 변조 모듈 로드 거부. 이를 "리버싱 난이도 / 리버싱이 어렵다"로 환산하지 않는다. 대외·문서 표현은 규칙 6을 따른다: "분석과 변조 비용을 높인다."

## 구현 게이트

`ROADMAP.md` Phase 0(현재)의 완료 조건은 `OPEN_QUESTIONS.md`의 **Q1~Q5(주력 호스트 2개 / 표면 문법 계열 / 메모리 모델 / 에러 모델 / 모듈 파일 포맷)가 닫히는 것**이다. 이것들이 닫히기 전에는 본격 구현을 시작하지 않는다.

## Git 워크플로 (PR 기반, 필수)

- 저장소: **GitHub Private — `xzawed/Mathless`**. 원격은 `origin`.
- **모든 변경은 PR로만 반영한다. `main`에 직접 커밋/푸시 금지.**
- 브랜치 접두어: `docs/*`(문서), `feat/*`(구현), `fix/*`(버그), `chore/*`(도구·메타).
- PR은 한 관심사로 좁히고, 관련 결정/질문 번호(예: `D16`, `Q12`)를 본문에 명시한다.
- PR 완료(머지) 선언 전, 규칙대로 근거 수준(E0/E1/E2)과 Grok 2차 검증(구현·진단·코드 검토는 필수)을 충족한다.
- `main`에는 squash-merge, 머지 후 브랜치 삭제. 자세한 규칙은 `CONTRIBUTING.md`.

## 개발 방법론 (SDD + WBS + TDD, 필수)

Phase 1부터 모든 구현 작업의 기본 절차. 순서를 건너뛰지 않는다.

1. **SDD (스펙 우선).** 코드 전에 스펙을 먼저 쓴다. 기능마다 `docs/phaseN/SPEC*.md`에 입력·출력·계약·측정 가능한 수용 기준을 정의하고, **사용자 확인 후** 구현한다. 스펙 없는 구현 금지.
2. **WBS (작업 분해).** 스펙을 PR 크기 작업으로 분해해 `docs/phaseN/WBS.md`에 의존 순서와 함께 관리한다. 각 작업 = 1 PR, 측정 가능한 완료 기준을 갖는다.
3. **TDD (테스트 우선).** 구현 전에 실패하는 테스트를 먼저 쓴다(Red → Green → Refactor). 테스트 결과가 곧 실측 근거(E2)다.
4. **Grok + 실측 (수행·검토 모두).** 각 작업의 수행과 검토를 Grok과 함께, 실측 데이터(테스트 결과·빌드 산출물·export 덤프·실행 로그) 기반으로 한다. PR 완료 전 `grok_build_verify` 필수.
5. 절차: **SPEC → (사용자 확인) → WBS → 작업별 [실패 테스트 → 구현 → 통과 → Grok 검증] → PR → merge.**

주장·완료 판단은 추측이 아니라 측정값으로 한다(E0/E1/E2 사다리는 "Grok 협업과 실측 기반 원칙" 참고). 측정 없이 "된다/빠르다/보호된다"라고 쓰지 않는다.

## 작업 우선순위

1. 열린 질문 중 MVP에 반드시 필요한 것만 닫기 (Q1~Q5)
2. 표면 문법 MVP 범위 확정
3. C ABI 초안 (로드 / export / 콜백 / 수명)
4. 최소 컴파일러 파이프라인 설계 (parse → IR → native)
5. Delphi 호스트 1개로 Hello 로드

## 산출물 규칙

- 설계 변경은 해당 `docs/*.md`를 **먼저** 수정한다.
- 코드가 생기면 `src/` 아래에 두고, 실험 코드와 제품 코드를 섞지 않는다. `ARCHITECTURE.md`가 권장하는 경계: `compiler/`(프론트엔드+IR), `backend/`(네이티브 codegen), `runtime/`(얇은 C ABI 런타임), `host/delphi`·`host/c`(바인딩), `packager/`(모듈 포맷).
- 추측은 `OPEN_QUESTIONS.md`로 보낸다. 문서 본문에 확정인 것처럼 쓰지 않는다.
- 확장자(`.mls`, `.mll`)와 C API 함수명은 모두 **가칭**이다. 확정된 것처럼 서술하지 않는다.

## 성공 기준 (초기)

호스트를 재컴파일하지 않고 Mathless로 컴파일한 네이티브 모듈을 로드해, 타입이 있는 함수(예: `discount(price, vip)`)를 호출할 수 있으면 1차 성공이다.
