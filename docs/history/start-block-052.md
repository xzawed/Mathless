> ### 2026-09-26 기준 (4) — **체크드 정수 산술 슬라이스가 닫혔다: `!` 본문의 오버플로가 `-3`이 됐다 (§9-70)**
>
> §9-69가 잰 1위 후보를 슬라이스로 닫았다(#333 SPEC · #334 구현). `!` 본문의 i32 `+ - *`·단항 `-`·`MIN / -1`과
> NaN·범위 밖 `f64 as i32`가 `ML_ST_OVERFLOW`(`-3`)를 내고, 실제 C 호스트가 그것을 받는다. 구현으로 다시 잰
> 626행은 실험의 표와 같다(조용한 오답 161행 중 101이 status). 같은 날 Q17은 (A)로 열어 두고 위험을
> `HOST_ABI.md`에 적었다(#335).
>
> **▶ 다음 세션 — 여기서 시작한다.** 순서대로:
>
> 1. **다음 슬라이스 — 사용자 결정.** 후보의 정본은 `docs/slices/README.md`의 "다음 슬라이스" 절이다. 체크드 산술 뒤에
>    남은 근거는 §9-70.2가 센다 — 여전히 조용한 60행(무한 헬퍼 16 · f64 절삭·NaN 14 · 산술이 아닌 것 25 …)과,
>    `-3`이 됐지만 참값이 i32에 드는 43행(정답을 주는 것은 **i64**뿐이다).
> 2. **작은 항목**: C# 호스트(Phase 4) · LSP(Phase 3, 큰 일).
> 3. **외부**: `X1`·`X2` — Delphi가 있는 CI 러너. 이 저장소 안에서는 닫히지 않는다.
>
> ⚠ **이 머신의 전체 게이트는 6회 중 2회 실패한다**(§9-63.5) — 실패를 보면 먼저 원인을 읽는다.
> 강한 신호는 CI 두 잡이다.
>
> 📌 **본문은 §9-70이다**(`docs/history/9-70.md`). 그 앞의 2026-09-26 (3) 항목(§7 열두 도메인)은
> 바이트 그대로 옮겼다 — 지금은 `docs/history/start-block-051.md`다.
>
